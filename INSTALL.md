# المحامي الشامل - دليل التثبيت للعملاء

## التثبيت السريع (5 دقائق)

### 1. المتطلبات
- Node.js 18+ أو Bun
- نظام تشغيل: Windows / macOS / Linux

### 2. التثبيت
```bash
# فك ضغط الملفات
# افتح Terminal/CMD في مجلد المشروع

# تثبيت الحزم
bun install
# أو: npm install

# تهيئة قاعدة البيانات (ينشئ قاعدة بيانات فارغة جديدة)
bun run db:push
# أو: npx prisma db push

# تشغيل النظام
bun run dev
# أو: npm run dev
```

### 3. أول تسجيل دخول
- افتح المتصفح: `http://localhost:3000`
- سجّل الدخول بـ:
  - **رمز PIN:** `1234`
  - أو **البريد:** `admin@lawyer.local` + **كلمة المرور:** `1234`

### 4. إعداد الذكاء الاصطناعي
1. اذهب إلى **الإعدادات → تبويب "الذكاء AI"**
2. أدخل:
   - **Base URL:** `https://api.freemodel.dev/v1` (أو أي مزود آخر)
   - **API Key:** مفتاحك الخاص
   - **Model:** `gpt-5.5` (أو أي نموذج آخر)
3. اضغط **"حفظ الإعدادات"**
4. اضغط **"اختبار الاتصال"**

### 5. إعداد تليجرام (اختياري)
1. على تليجرام، ابحث عن `@BotFather`
2. أرسل `/newbot` وأنشئ بوتاً جديداً
3. انسخ **Bot Token**
4. في النظام: **الإعدادات → تليجرام**
5. الصق الرمز واضغط **"حفظ"**
6. افتح البوت وأرسل `/start`
7. انسخ معرف الشات وأضفه في **"المعرفات المصرح لها"**

---

## ⚠️ ملاحظات مهمة

### البيانات منفصلة تماماً:
- كل تثبيت له **قاعدة بيانات خاصة** (ملف `db/custom.db`)
- **لا توجد بيانات مشتركة** بين العملاء
- إعداداتك (AI، تليجرام) محفوظة في **قاعدة بياناتك فقط**

### ما يحتاجه كل عميل:
| العنصر | المصدر | ملاحظة |
|--------|--------|--------|
| **مفتاح AI** | من مزود الذكاء الاصطناعي | كل عميل يحصل على مفتاحه الخاص |
| **Bot Token** | من `@BotFather` على تليجرام | كل عميل ينشئ بوتاً خاصاً |
| **Chat ID** | من البوت بعد `/start` | معرف تليجرام الخاص بالعميل |
| **قاعدة البيانات** | تُنشأ تلقائياً | فارغة - بيانات العميل فقط |

### الأمان:
- جميع البيانات مخزنة **محلياً** على جهاز العميل
- لا توجد اتصالات خارجية إلا:
  - مزود الذكاء الاصطناعي (عند استخدام AI)
  - تليجرام (عند استخدام البوت)
- كلمات المرور مشفرة (SHA-256)

---

## استكشاف الأخطاء

### "حدث خطأ أثناء تسجيل الدخول"
```bash
bun run db:push
```

### "المفكر القانوني لا يعمل"
- تأكد من إعداد مفتاح AI في الإعدادات

### "البوت لا يستجيب"
1. تأكد من تشغيل خدمة البوت: `cd mini-services/telegram-bot && bun run dev`
2. تأكد من إدخال Bot Token صحيح
3. تأكد من إضافة معرف الشات في القائمة المصرح لها

---

## الدعم الفني
للاستفسارات، تواصل مع المطور.
